function CRC32(data) {
	int divisor = 1011;
	int new_data;
	for (i < int.toString().length) {
		for (j < divisor) {
			if (data[i + j] == divisor[j]) {
				new_data[i + j] = 0;
				} else {new_data[i + j] = 1}
			data[i + j] = new_data[i + j];
		} 
	}	
}